# კონტრიბუციის წესები (CONTRIBUTING)

ეს დოკუმენტი აღწერს, თუ როგორ უნდა იმუშავოს გუნდის წევრმა ამ რეპოზიტორიაში. ყველა კონტრიბუტორი ვალდებულია დაიცვას ეს წესები.

---

## 1. ძირითადი პრინციპები

- **არასოდეს** არ ჩაკომიტოთ პირდაპირ `main` branch-ში.
- ყოველი ცვლილება ხდება მხოლოდ **Pull Request**-ის გავლით.
- ყოველი PR საჭიროებს **მინიმუმ 1 review**-ის დადასტურებას.
- კონფლიქტი უნდა მოაგვაროს PR-ის **ავტორმა**, არა reviewer-მა.

---

## 2. Branch-ების დასახელება

გამოიყენეთ შემდეგი პრეფიქსები:

| პრეფიქსი | რისთვის | მაგალითი |
|---|---|---|
| `feature/` | ახალი ფუნქციონალი | `feature/giorgi-profile` |
| `fix/` | შეცდომის გასწორება | `fix/team-md-typo` |
| `docs/` | მხოლოდ დოკუმენტაცია | `docs/update-readme` |
| `chore/` | ტექნიკური სამუშაო | `chore/add-gitignore` |

**წესები:**
- მხოლოდ პატარა ასოები (lowercase).
- სიტყვები გამოყავით `-`-ით (kebab-case).
- გამოიყენეთ ლათინური ასოები, არა ქართული.

---

## 3. Commit Message-ის ფორმატი

ჩვენ ვიყენებთ [Conventional Commits](https://www.conventionalcommits.org/) სტანდარტს:

```
<type>: <მოკლე აღწერა ქართულად>
```

**ტიპები:**

- `feat:` — ახალი ფუნქციონალი
- `fix:` — შეცდომის გასწორება
- `docs:` — დოკუმენტაციის ცვლილება
- `style:` — ფორმატირება (კოდის ლოგიკა არ იცვლება)
- `refactor:` — კოდის გადაწერა ფუნქციონალის შენარჩუნებით
- `chore:` — ტექნიკური სამუშაო

**მაგალითები:**

✅ კარგი:
```
feat: დაემატა გიორგის პროფილი members/-ში
fix: მოგვარდა merge conflict team.md-ში
docs: განახლდა README ჩაბარების ვადით
```

❌ ცუდი:
```
update
fix bug
ცვლილებები
asdf
```

---

## 4. სამუშაო პროცესი (Workflow)

### ნაბიჯი 1: სვეჟი main

ყოველი ახალი მუშაობის დაწყებამდე:

```bash
git checkout main
git pull origin main
```

### ნაბიჯი 2: შექმენი branch

```bash
git checkout -b feature/<შენი-სახელი>
```

### ნაბიჯი 3: იმუშავე და დააკომიტე

```bash
git add <ფაილები>
git commit -m "feat: მოკლე აღწერა"
```

⚠️ **არ გამოიყენოთ** `git add .` — დაამატეთ მხოლოდ ის ფაილები, რომელიც გსურთ.

### ნაბიჯი 4: Push

```bash
git push origin feature/<შენი-სახელი>
```

### ნაბიჯი 5: გახსენი Pull Request

- სათაური უნდა იყოს ნათელი (იგივე ფორმატი როგორც commit message).
- აღწერაში მიუთითეთ:
  - **რა** შეცვალეთ
  - **რატომ** შეცვალეთ
  - **როგორ** შეგიძლიათ შემოწმოთ (testing instructions)
- მონიშნეთ მინიმუმ ერთი reviewer.

### ნაბიჯი 6: უპასუხეთ review-ის კომენტარებს

- ყველა კომენტარი უნდა იყოს დახურული merge-მდე.
- თუ არ ეთანხმებით — ახსენით თქვენი არგუმენტი, არ უგულებელყოთ.

### ნაბიჯი 7: Merge

- გამოიყენეთ **"Squash and merge"** მცირე PR-ებისთვის.
- გამოიყენეთ **"Merge commit"** დიდი PR-ებისთვის, სადაც commit-ების ისტორია მნიშვნელოვანია.
- Merge-ის შემდეგ წაშალეთ branch.

---

## 5. Code Review-ის წესები

### Reviewer-ის ვალდებულებები:

- გადახედეთ PR-ს **24 საათში**.
- დატოვეთ **კონსტრუქციული** კომენტარები — ახსენით **რატომ**, არა მხოლოდ **რა**.
- გამოიყენეთ თავაზიანი ენა — ეს არის სასწავლო პროცესი.
- დაამტკიცეთ (Approve) მხოლოდ მაშინ, თუ კოდი მზად არის.

### კარგი კომენტარის მაგალითი:

> ეს ცვლადი უკეთ წაიკითხება, თუ `x`-ის ნაცვლად დაარქმევ `userName`-ს. ეს ხელს შეუწყობს კოდის გასაგებობას.

### ცუდი კომენტარის მაგალითი:

> არასწორია.

---

## 6. Merge Conflict-ის მოგვარება

თუ თქვენი branch conflict-შია `main`-თან:

```bash
git checkout main
git pull origin main
git checkout feature/<შენი-სახელი>
git merge main
# გაასწორეთ conflict-ები ფაილებში
git add <გასწორებული-ფაილები>
git commit -m "fix: მოგვარდა merge conflict"
git push origin feature/<შენი-სახელი>
```

⚠️ conflict-ის მოგვარების შემდეგ, **ხელახლა მოითხოვეთ review**.

---

## 7. რა არ უნდა გააკეთოთ

- ❌ `git push --force` `main`-ზე.
- ❌ სხვისი PR-ის merge-ი მისი თანხმობის გარეშე.
- ❌ პირადი მონაცემების commit (პაროლები, ტოკენები, `.env` ფაილები).
- ❌ დიდი ბინარული ფაილების commit (სურათები, ვიდეო) git-ის გარეშე LFS-ისა.
- ❌ commit message-ის გარეშე ცვლილებები (`-m ""` ან `-m "."`).

---

## 8. დახმარება

თუ რამე გაუგებარია:

1. ჯერ წაიკითხეთ `README.md`.
2. შემდეგ — ეს `CONTRIBUTING.md`.
3. ჰკითხეთ გუნდის ლიდერს PR-ის კომენტარში ან Issue-ში.

წარმატებებს გისურვებთ! 🚀
